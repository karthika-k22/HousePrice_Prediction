# HousePrice_Prediction
This project predicts house prices using the Ames Housing dataset. It applies data preprocessing (median imputation, scaling) and a Gradient Boosting Regressor within a Scikit-learn pipeline. The model is evaluated using RMSE and R² score. Users can input house features to get real-time price predictions.
